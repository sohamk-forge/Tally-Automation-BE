{
  "name": "tally-sync",
  "version": "1.0.0",
  "description": "Tally to Database Sync Application",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate:latest": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback",
    "migrate:status": "knex migrate:status",
    "migrate:make": "knex migrate:make",
    "seed:run": "knex seed:run",
    "db:reset": "npm run migrate:rollback && npm run migrate:latest",
    "migrate:users-to-supertokens": "node scripts/migrate-users-to-supertokens.js",
    "deploy": "git pull && npm install && npm run migrate:latest && (pm2 reload tally-backend --update-env || pm2 start src/server.js --name tally-backend)"
  },
  "dependencies": {
    "@bull-board/api": "^7.1.5",
    "axios": "^1.18.1",
    "bcryptjs": "^3.0.3",
    "bullmq": "^5.78.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "fast-xml-parser": "^5.9.3",
    "form-data": "^4.0.6",
    "ioredis": "^5.11.1",
    "knex": "^3.2.9",
    "multer": "^2.2.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^9.0.3",
    "pdf-to-img": "^6.2.0",
    "pg": "^8.20.0",
    "puppeteer": "^25.4.0",
    "supertokens-node": "^24.0.3",
    "xlsx": "^0.18.5",
    "xml2js": "^0.6.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "loadtest": "^8.2.1",
    "nodemon": "^3.1.0"
  }
}
