/**
 * @param {import('knex')} knex
 */
export async function up(knex) {
  // Original migration file was lost. This is a placeholder stub
  // recreated to satisfy knex's migration history validation.
  // This migration was already applied previously — up() will NOT
  // run again since it's already recorded in knex_migrations.
}

/**
 * @param {import('knex')} knex
 */
export async function down(knex) {
  // No-op — original down() logic is unknown/lost.
}