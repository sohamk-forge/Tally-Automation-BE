import { syncQueue } from "../queues/syncQueue.js";

export async function startScheduler() {
  // Auto sync DISABLED - Manual sync only
  console.log("🔄 Auto sync disabled. Use manual sync APIs.");
}