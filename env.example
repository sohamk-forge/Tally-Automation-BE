DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=tally_sync
DB_SCHEMA=app_test
PORT=5001
TALLY_URL=http://localhost:9000
NODE_ENV=development

FRONTEND_URL=http://localhost:5173
API_DOMAIN=http://localhost:5001

# SuperTokens (self-hosted core, see docker-compose.yml)
SUPERTOKENS_CONNECTION_URI=http://localhost:3567
SUPERTOKENS_API_KEY=generate_with_openssl_rand_hex_32
# Dedicated least-privilege Postgres role, not the app's main DB_USER/DB_PASSWORD
SUPERTOKENS_DB_USER=supertokens_core
SUPERTOKENS_DB_PASSWORD=generate_with_openssl_rand_hex_24

# Unused today, kept for a possible future EmailVerification recipe
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
